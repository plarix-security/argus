  argus v0.6.0  ·  Plarix
  ─────────────────────────────────────────────────────

  Scanning  examples/langgraph-production

  ─────────────────────────────────────────────────────
  4 critical  ·  18 warning  ·  12 info
  ─────────────────────────────────────────────────────

  ● CRITICAL shell.py:56
    subprocess.run(
    Shell command execution. No policy gate in call path.

  ● CRITICAL shell.py:125
    file_path.unlink()
    Recursive directory deletion. No policy gate in call path.

  ● CRITICAL shell.py:131
    shutil.rmtree(target_dir)
    Recursive directory deletion. No policy gate in call path.

  ● CRITICAL shell.py:254
    subprocess.run(
    Shell command execution. No policy gate in call path.

  ● WARNING  data_pipeline_agent.py:74
    requests.post(
    HTTP POST to external endpoint. No policy gate in call path.

  ● WARNING  data_pipeline_agent.py:213
    temp_path.parent.mkdir(parents=True, exist_ok=True)
    Directory creation. No policy gate in call path.

  ● WARNING  data_pipeline_agent.py:214
    temp_path.write_text(json.dumps(data))
    File write operation. No policy gate in call path.

  ● WARNING  crm.py:94
    requests.patch(
    HTTP mutation to external endpoint. No policy gate in call path.

  ● WARNING  crm.py:104
    requests.post(
    HTTP POST to external endpoint. No policy gate in call path.

  ● WARNING  crm.py:171
    requests.post(
    HTTP POST to external endpoint. No policy gate in call path.

  ● WARNING  email.py:91
    server.sendmail(email_config.from_address, all_recipients...
    External API mutation. No policy gate in call path.

  ● WARNING  email.py:147
    server.sendmail(email_config.from_address, [email_addr], ...
    External API mutation. No policy gate in call path.

  ● WARNING  files.py:87
    full_path.parent.mkdir(parents=True, exist_ok=True)
    Directory creation. No policy gate in call path.

  ● WARNING  files.py:89
    full_path.write_text(content)
    File write operation. No policy gate in call path.

  ● WARNING  files.py:213
    log_path.parent.mkdir(parents=True, exist_ok=True)
    Directory creation. No policy gate in call path.

  ● WARNING  files.py:218
    with open(log_path, "a", encoding="utf-8") as f:
    File system modification. No policy gate in call path.

  ● WARNING  files.py:219
    f.write(log_entry)
    File write operation. No policy gate in call path.

  ● WARNING  shell.py:54
    Path(work_dir).mkdir(parents=True, exist_ok=True)
    Directory creation. No policy gate in call path.

  ● WARNING  shell.py:166
    workspace_path.mkdir(parents=True, exist_ok=True)
    Directory creation. No policy gate in call path.

  ● WARNING  shell.py:174
    shutil.copy2(file_path, workspace_path)
    File copy/move operation. No policy gate in call path.

  ● WARNING  tickets.py:77
    requests.post(
    HTTP POST to external endpoint. No policy gate in call path.

  ● WARNING  tickets.py:134
    requests.put(
    HTTP mutation to external endpoint. No policy gate in call path.

  ● INFO     crm.py:77
    requests.get(
    External HTTP read. No policy gate in call path.

  ● INFO     crm.py:133
    requests.get(
    External HTTP read. No policy gate in call path.

  ● INFO     database.py:61
    cursor.execute(query, (customer_id,))
    Database query execution. No policy gate in call path.

  ● INFO     database.py:62
    cursor.fetchone()
    Database read operation. No policy gate in call path.

  ● INFO     database.py:97
    cursor.execute(query, params or [])
    Database query execution. No policy gate in call path.

  ● INFO     database.py:101
    cursor.fetchall()
    Database read operation. No policy gate in call path.

  ● INFO     database.py:161
    cursor.execute(query, values)
    Database query execution. No policy gate in call path.

  ● INFO     files.py:43
    full_path.read_text(encoding=encoding)
    File read operation. No policy gate in call path.

  ● INFO     files.py:129
    with open(full_path, "r", newline="", encoding="utf-8") a...
    File read operation. No policy gate in call path.

  ● INFO     files.py:177
    for file_path in base_path.glob(pattern):
    Directory listing. No policy gate in call path.

  ● INFO     shell.py:116
    for file_path in target_dir.glob(pattern):
    Directory listing. No policy gate in call path.

  ● INFO     tickets.py:176
    requests.get(
    External HTTP read. No policy gate in call path.

  ─────────────────────────────────────────────────────
  12 files  ·  1.0s  ·  AFB04 coverage only
